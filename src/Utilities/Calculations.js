// Helper Functions for calculating Macros, Totals, and Stats //

// Add Macro Units from any given object
const addUnits = () => {

    

    // let total = 0;
    // for (var key in unitsObject) {
    //     total += unitsObject[key];
    // };

    // return total;

};

// Total Macro Amount
const totalUnits = () => {

};
